# C 언어 수업 정리

---

# 1. 변수(Variable)

변수는 데이터를 저장하는 메모리 공간이다.

```text
int a = 10;
```

위 코드는 정수형 변수 `a`를 만들고 10을 저장한다.

## 핵심

* 변수는 데이터를 저장한다.
* 자료형에 따라 저장 가능한 값이 다르다.
* 가장 많이 사용하는 자료형은 `int`이다.

| 자료형    | 설명   |
| ------ | ---- |
| int    | 정수   |
| float  | 실수   |
| double | 큰 실수 |
| char   | 문자   |

## 예제

```c
int a = 10;
int b = 20;

printf("%d", a + b);
```

---

# 2. 산술 연산자

수학 계산을 수행하는 연산자이다.

| 연산자 | 의미  |
| --- | --- |
| +   | 덧셈  |
| -   | 뺄셈  |
| *   | 곱셈  |
| /   | 몫   |
| %   | 나머지 |

## 핵심

`/` 는 몫을 구한다.

```c
7 / 3 = 2
```

`%` 는 나머지를 구한다.

```c
7 % 3 = 1
```

## 예제

```c
int a = 7;
int b = 3;

printf("%d\n", a / b);
printf("%d\n", a % b);
```

---

# 3. 증감 연산자

변수의 값을 1 증가 또는 감소시킨다.

## 종류

```c
a++;
```

사용 후 증가

```c
++a;
```

증가 후 사용

## 핵심

시험에서는 출력 결과를 묻는 문제가 자주 나온다.

## 예제

```c
int a = 3;

printf("%d\n", a++);
printf("%d\n", a);
```

출력

```text
3
4
```

---

# 4. scanf와 printf

## printf

화면에 출력한다.

### 형식 지정자

| 형식 | 의미  |
| -- | --- |
| %d | 정수  |
| %f | 실수  |
| %c | 문자  |
| %s | 문자열 |

### 예제

```c
int a = 100;

printf("%d", a);
```

---

## scanf

사용자로부터 입력받는다.

### 핵심

반드시 주소를 넘겨야 한다.

```c
scanf("%d", &a);
```

`&` 는 변수의 주소를 의미한다.

### 예제

```c
int a;

scanf("%d", &a);

printf("%d", a);
```

---

# 5. 조건문(if)

조건이 참일 때만 실행된다.

## 구조

```c
if(조건)
{
    실행문;
}
```

## 예제

```c
if(a > 0)
{
    printf("양수");
}
```

---

# 6. if - else

둘 중 하나를 선택할 때 사용한다.

## 구조

```c
if(조건)
{
}
else
{
}
```

## 예제

```c
if(a > b)
{
    printf("%d", a);
}
else
{
    printf("%d", b);
}
```

---

# 7. 논리 연산자

조건을 여러 개 사용할 때 사용한다.

## AND

```c
&&
```

두 조건 모두 참이어야 참

### 예제

```c
if(a > 0 && a < 100)
{
    printf("참");
}
```

---

## OR

```c
||
```

하나만 참이어도 참

### 예제

```c
if(a == 1 || a == 2)
{
    printf("참");
}
```

---

## NOT

```c
!
```

참과 거짓을 반대로 만든다.

### 예제

```c
if(!(a == 10))
{
    printf("10이 아님");
}
```

---

# 8. 삼항 연산자

간단한 if문을 한 줄로 표현한다.

## 구조

```c
조건 ? 참 : 거짓
```

## 예제

```c
printf("%s",
       age < 19 ? "미성년자" : "성인");
```

---

# 9. switch 문

선택지가 많을 때 사용한다.

## 구조

```c
switch(값)
{
case 1:
    break;

case 2:
    break;

default:
    break;
}
```

## 핵심

`break` 가 없으면 다음 case도 실행된다.

## 예제

```c
switch(sel)
{
case 1:
    printf("덧셈");
    break;

case 2:
    printf("뺄셈");
    break;
}
```

---

# 10. 반복문(for)

같은 작업을 여러 번 반복한다.

## 구조

```c
for(초기식; 조건식; 증감식)
{
}
```

## 예제

```c
for(int i = 1; i <= 10; i++)
{
    printf("%d\n", i);
}
```

---

## 누적합

시험에 자주 나오는 유형

### 예제

```c
int sum = 0;

for(int i = 1; i <= 100; i++)
{
    sum += i;
}
```

---

# 11. while 문

조건이 참인 동안 반복한다.

## 구조

```c
while(조건)
{
}
```

## 예제

```c
while(a != 0)
{
    scanf("%d", &a);
}
```

---

## break

반복문을 즉시 종료한다.

### 예제

```c
if(a == 0)
{
    break;
}
```

---

# 12. 배열(Array)

같은 자료형 여러 개를 저장하는 공간이다.

## 선언

```c
int arr[5];
```

## 핵심

배열 인덱스는 0부터 시작한다.

```text
arr[0]
arr[1]
arr[2]
arr[3]
arr[4]
```

## 예제

```c
arr[0] = 10;
arr[1] = 20;
```

---

# 13. 배열과 반복문

배열은 반복문과 함께 사용한다.

## 예제

```c
for(int i = 0; i < 5; i++)
{
    printf("%d\n", arr[i]);
}
```

---

# 14. 최대값 찾기

배열 문제의 대표 유형

## 예제

```c
int max = arr[0];

for(int i = 1; i < n; i++)
{
    if(max < arr[i])
    {
        max = arr[i];
    }
}
```

---

# 15. 문자열

문자의 배열이다.

## 선언

```c
char name[] = "Korea";
```

실제로는

```text
K o r e a \0
```

형태로 저장된다.

## 예제

```c
printf("%s", name);
```

---

# 16. 함수(Function)

코드를 재사용하기 위해 만든다.

## 예제

```c
void add()
{
    printf("덧셈");
}
```

사용

```c
add();
```

---

# 17. 포인터(Pointer)

주소를 저장하는 변수이다.

## 주소 구하기

```c
&a
```

## 포인터 선언

```c
int *p;
```

## 예제

```c
int a = 10;

int *p = &a;

printf("%d", *p);
```

출력

```text
10
```

---

# 시험 전 필수 암기

```text
/  : 몫
%  : 나머지
&  : 주소
*  : 포인터

if
if else
switch
for
while

배열
최대값 찾기
합계 구하기
개수 세기

함수
포인터
```
