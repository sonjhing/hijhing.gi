# 0514
## 이미지 인식 
* 사진을 학습시키기
* 이미지 = 픽셀로 이루어짐, 인식할 때 숫자로 받아들임, RGB 숫자 3개가 동시에 들어와야 함
* 숫자 정보를 올바르게 인식해준다면 형태 형성 가능
* 순전파(가중치 거쳐 예측값 계산), 역전파(오차를 뒤로 보내며 가중치 수정), 로스계산(정답과의 오차 구하기) 

## 딥러닝을 활용한 옷 분류기 
* MNIST 데이터셋은 6만 개의 트레인 데이터, 10만 개의 테스트 데이터
* 만약 FHD나 UHD 해상도라면 1960*1000 같은 엄청난 픽셀이 생겨남
* 이러면 딥러닝 모델로 연산해야 하는 숫자도 너무 많고, 학습을 위해 훨씬 더 많은 데이터가 필요해짐
* 그래서 기본 이미지 인식 연습용으로는 28*28 크기의 작은 이미지를 픽셀로 점 찍어서 만들고 정확한 데이터를 준비함

## TVT (데이터 분할)
* **Train:** 모델을 실제로 학습시키는 데이터 (수백만 번 학습 진행, 전체의 약 70%)
* **Validation:** 모델이 오버피팅되지 않고 학습 방향이 잘 나아가고 있는지 검증 (10~15%)
* **Test:** 학습 완료 후 최종 성능을 테스트하기 위해 일반적으로 딱 한 번만 사용 (10~15%)

* **Batch:** 모델이 한 번에 연산하고 가중치를 업데이트하는 데이터의 양 (메모리를 위해 적절히 나눠주어야 학습 가능)
* **Epoch:** 모델이 학습 데이터 전체를 몇 번 반복해서 회독할 것인지 설정하는 횟수

* **문1)** 학습 데이터 1000개, 배치 50, 에폭 10일 때 배치는 몇 번 반복(이테레이션)되는가?
  * **답)** 1000 / 50 * 10 = 200번 반복
* **문2)** 모델이 각 데이터는 총 몇 번 확인하는가?
  * **답)** 에폭 수와 동일하므로 10번 확인
* 학습 완료 후 모델은 주로 `.h5` 파일 형식으로 저장함

## CNN (합성곱 신경망)
* 이미지의 공간적 특징을 유지한채 압축하는 모델
* **커널(필터):** 5*5 또는 3*3 크기의 격자로 이미지 위를 훑음
* **Feature Map (피처맵):** 원본 이미지 위에 커널을 통과시켜 특징들을 잘 모아 만든 새로운 레이어
* 내가 추출하려고 하는 이미지의 특징과 일치하는 부분을 만나면 결과 숫자가 높아짐 (높은 숫자가 많을수록 판별력이 올라감)
* 너무 많은 커널을 연속으로 통과시킬 때, 한 칸씩 갈지 두 칸씩 갈지(스트라이드)나 개수, 깊이를 잘 조절해야 함

* **Pooling (풀링):** 객체의 위치가 조금 달라져도 강건(외풍이나 변화에 버티는 힘)해질 수 있도록 다운샘플링(압축)하는 추가 작업
* 원본 특징을 모은 피처맵에 풀링을 통과시키면 최종 풀링 레이어가 나옴
  * **Max Pooling:** 지정한 영역 안에서 가장 큰 최댓값 하나만 추출 (가장 강한 특징만 남김, 주로 사용)
  * **Average Pooling:** 지정한 영역의 평균값을 구함

* **전체 흐름:** Input Image -> Kernels 통과 -> Feature Map (커널 개수만큼 생성) -> Pooling (압축) -> Flattening (1차원으로 길게 펼치기) -> 최종 이미지 분류 및 물체 탐지 수행

* **Hugging Face:** 트랜스포머 모델이나 데이터셋 같은 머신러닝 프레임워크와 오픈소스를 제공하는 세계 최대의 인공지능 플랫폼
* **GitHub:** 완성된 코드와 프로젝트를 올려서 관리하는 저장소

---

# 0514 추가 (포트폴리오 페이지 구현 내용)
* 페이지를 구성하기 위한 필수 요소를 제외하고 불필요한 모든 파일 삭제 (GitHub Pages 배포 환경 세팅 목적)
* **기능설명:** 1. 메인 진입 시 글자가 마치 물 번지듯이(수채화/잉크 확산 효과) 살짝 나타나는 로딩 애니메이션 구현
  2. 화면 중앙에 'PORTFOLIO' 글자가 각각 컬러풀하게 배치되며, 각 글자를 클릭하면 서로 다른 개별 페이지로 링크 이동
  3. 텍스트가 없는 배경 영역을 클릭하면 화면에 은은하게 빛나는 리플(파동) 효과 발생
  4. 기존의 사과 이미지 대신 귤/오렌지 이미지를 배경 감성으로 활용
  5. 어두운 배경 스타일과 화려한 타이포그래피, 대담한 텍스트를 혼합한 스타일 레이아웃 구성
  6. 화면 한쪽에 "FIGHTING 100%", "안녕하세요!" 같은 라벨 추가
  7. config 파일 수정 및 JS 에러 없는 깔끔한 코드로 세팅 완료

---

# 0521
## 자연어 처리 (NLP) 딥러닝 모델
* 쉽게 말해 문장 속 빈칸찾기 같은 느낌으로 작동함
* **인코딩:** 문자를 컴퓨터가 연산할 수 있도록 숫자로 바꾸는 과정
* **원-핫 인코딩 (One-Hot Encoding):** 문자를 1대1로 대응시켜 표현하는 방식 (단어 하나당 하나의 자리만 1이고 나머진 0)
  * 연산을 수행해서 확률 표로 바뀌는데 단어 수가 많아지면 연산이 매우 어려워짐
  * 또한 유사한 의미를 가진 언어라도 컴퓨터는 완전하게 다른 별개의 단어로 취급하는 치명적인 단점이 있음 (의미적 유사도 반영 불가)

## RNN (순환 신경망)
* 자연어는 단어의 배열 순서가 중요하므로, 문맥적 순서를 기억하는 것에 중점을 둔 모델
* **구조:** 기존의 예측값(과거 정보)이 다음 시점의 Cell 입력값과 함께 들어감
* 1회차 rnn 예측값 -> 2회차 cell 입력값 -> n회차 예측값으로 누적 전달됨
* 이론적으로는 앞에 있던 입력 정보가 절대 사라지지 않고 계속 뒤로 전달되는 구조임
* **문제점:** 문장이 너무 길어지면 충분한 기억력을 유지하지 못함 (장기 의존성 문제, 앞의 정보를 까먹음)

## LSTM (장단기 메모리)
* RNN의 기억력 문제를 해결하기 위해 나옴 (이전 장기 기억, 이전 예측값, 현재 입력값을 모두 고려)
* 불필요한 기억은 지우고 새로운 중요한 구조만 기억하게 제어함
* **Forget Gate** (이전 기억을 얼마나 지울지), **Input Gate** (새 정보를 얼마나 저장할지), **Output Gate** (어떤 예측값을 내보낼지) 구조로 작동
* 이전 단계에서 입력한 연산값을 합쳐서 일부는 장기 기억에서 제거하고, 일부는 장기 기억 공간(Cell State)에 저장
* 장기 기억 상태와 단기 예측 상태 2가지를 한 번에 저장하며 흐르기 때문에, 문장에서 어떤 중요한 정보를 계속 유지시킬지 ai가 스스로 제어할 수 있게 됨

* **임베딩 (Embedding):** 텍스트를 모델이 이해할 수 있도록 의미가 담긴 숫자로 변환하는 기술 ('아이들이'와 '어린이들이'의 의미적 유사성을 파악할 수 있게 만듦)
  * **Sparse Representation (희소 표현):** 원-핫 인코딩처럼 대부분의 값이 0이고 특정 자리만 1인 형태, 단어 간 유사도가 낮게 나옴
  * **Dense Representation (밀집 표현):** 요즘 주로 사용하는 방식, 단어를 수십~수백 차원의 연속된 실수 공간에 매핑함
  * 의미적으로 비슷한 단어들은 고차원 공간 안에서 가까운 특정 공간에 뭉치게 되며, 벡터 값의 (+, -) 방향성과 크기로 단어 뜻이 매핑되어 유사도가 높게 측정됨

## 생성형 AI로 프로그래밍 가이드
* **프롬프트 개선:** 단순 질문이 아니라 "나는 이런 프로그램을 만들고 구조는 이렇게 잡았어"라는 명확한 가이드 문서를 제공해야 성능이 올라감
* 프로젝트의 목적, 폴더 구조, 연산 제약사항(파라미터 크기 등)을 명시해야 함
* 대형 모델(LLM)은 내부 Cell에 들어있는 파라미터가 조 단위에 달함. 글자 한 자를 출력하기 위해서도 엄청난 조 단위의 연산을 진행해야 하므로 컨텍스트를 잘 잡아주는 것이 필수임
* 이러한 내용을 바탕으로 프로젝트의 가이드 지침서인 `claude.md` 파일 등을 구조화하여 만들어 활용함

---

# 0528
## RAG (검색 증강 생성)
* **Document Retrieval (문서 검색) 시스템 기반**
* LLM은 정확한 정보 전달이 목표이지만, 학습하지 않은 내용에 대해 그럴듯하게 거짓말을 하는 할루시네이션(환각) 현상을 일으켜 오정보를 전달하는 취약점이 있음
* 이를 해결하기 위해 사용자의 질문에 답변하는 데 필요한 실제 지식 정보를 외부 DB에서 적절하게 추출해오는 시스템을 연동함
* 과거에는 단순 형태소 매핑 검색이었으나, 지금은 자동으로 의미적 유사도를 계산하는 RAG 시스템이 돌아감 (질문과 가장 유사한 맥락을 가진 내부 문서를 찾아 필요한 정보만 쏙 뽑아 답변에 활용)

* **R (Retrieval - 검색):** 외부 데이터 소스 및 문서에서 질문과 관련된 정보 검색
* **A (Augmented - 증강):** 검색한 결과를 기존 사용자 질문과 함께 길게 엮어서 프롬프트를 확장(증강)함
* **G (Generation - 생성):** 증강된 풍부한 참조 문서를 바탕으로 LLM이 최종적으로 정확하고 더 좋은 답변을 생성함

* **전체 프로세스:** 사용자 질문 -> 1. 챗 엔진이 관련 문서 DB 검색 -> 2. 활용할 데이터 소스 확보 -> 3. AI에게 질문과 관련 문서를 길게 붙여서 프롬프트 입력 -> 4. AI가 문서를 기반으로 정확한 답변 생성

## Fine-Tuning(파인튜닝) vs RAG(검색 증강 생성)
* **파인튜닝:** 특정 도메인에 맞춰 모델 자체를 최적화하는 방식, 모델의 가중치(Weight)를 데이터를 줘서 직접 업데이트하며 도메인 지식을 학습시킴
* **RAG:** 모델의 가중치를 바꾸지 않고, 최신 정보나 특정 사내 지식을 외부 DB에서 실시간으로 검색하여 응답을 생성하게 함 (내가 가진 지식 문서를 모델에게 참고용으로 던져주고 올바른 지식 안에서만 정답을 찾아내게 만드는 방식)

## Retrieval 과정 메커니즘
* "How to do X?" 질문 입력 -> **Embedding Machine** (중요: 질문을 숫자로 변환) -> Embedding 벡터 생성 -> **Vector Database**와 대조 -> 가장 유사한 **Relevant Snippets** (연관 문서 조각) 추출

## Generation 과정 메커니즘
* "How do I do X?" 질문 + 추출된 **Relevant Snippets** (참조 문서) -> LLM 입력 -> **Augmented Answer** (문서에 기반한 정확한 최종 답변) 생성

## LangChain (랭체인) 이란?
* 거대 언어 모델(LLM)로 구동되는 애플리케이션 개발을 위한 전용 오픈소스 프레임워크
* 마치 사슬(Chain)처럼 여러 기능을 서로 엮어서 아주 쉽고 빠르게 LLM 기반 앱을 만들 수 있게 도와줌
* **데이터 인식:** 언어 모델을 다른 외부 데이터 소스(PDF, DB, 웹페이지)에 연결하는 도구 제공
* **에이전트 기능:** 언어 모델이 단순히 텍스트만 뱉는 게 아니라 실제 환경이나 툴과 상호작용하도록 제어

## ChatGPT의 특성과 한계 극복
1. ChatGPT 자체는 특정 과거 시점(예: 2023년 4월 등)까지 학습한 데이터만 알고 있음 -> 이를 해결하기 위해 VectorStore 기반 정보 탐색(RAG)을 붙여 최신 정보를 알게 만듦
2. **토큰 제한 문제:** 입력 및 출력에 글자 수 제한이 존재함 -> TextSplitter를 활용해 긴 문서를 작은 단위로 분할하여 필요한 부분만 입력되게 처리
3. **할루시네이션 극복:** "주어진 문서 안에서만 답변하고, 모르면 모른다고 해라"라는 제약 Prompt를 입력하여 방지

## LangChain의 6대 핵심 구성 요소
* **LLM:** 초거대 언어 모델 그 자체, 생성 모델의 핵심 엔진이며 좋은 모델을 쓸수록 전체 앱 성능 업그레이드됨
* **Prompts:** 언어 모델에게 내리는 지시 명령문 및 템플릿 관리
* **Index:** LLM이 외부 문서를 쉽게 탐색할 수 있도록 데이터를 구조화하고 저장하는 공간 (Vector Store 등)
* **Memory:** LLM과 사용자가 대화하는 동안 나눈 이전 정보들을 포함하고 기억하여 향후 대화에 컨텍스트를 유지하는 기능
* **Chain:** LLM 호출을 사슬처럼 연결하여 여러 작업을 연속적으로 실행할 수 있도록 엮어주는 구조 (동일 입력에 대해 여러 모양으로 작업 연계 가능)
* **Agents:** LLM이 기존의 프롬프트 지시만으로 수행할 수 없는 작업(예: 직접 웹 검색하기, 계산기 돌리기 등)을 스스로 판단해서 도구를 선택해 수행하게 만드는 기능

* **LLM 추상화:** 사용자에게 불필요한 내부의 복잡한 세부사항(API 호출, 토큰 연산 등)을 숨기고 간결하게 처리하는 것. 개발자는 제공된 추상화 인터페이스 위에서 나만의 비즈니스 로직을 쉽게 구현 가능

## LangChain을 활용한 PDF 챗봇 작동 원리 (RAG 체인 빌드업)
1. **Document Loader:** PDF 등 외부 문서를 시스템으로 업로드하여 가져옴
2. **Text Splitter (문서 분할):** 토큰 제한이 있는 LLM을 위해 긴 문서를 Chunk(청크) 단위로 쪼갬. 이때 문맥이 끊기는 걸 막기 위해 앞뒤 청크를 조금씩 겹치게 만드는 Chunk Overlap 설정을 적용함
3. **문서 임베딩:** 대용량 말뭉치로 사전 학습된 모델을 사용해 쪼개진 텍스트 청크들을 숫자로 수치화(임베딩)함. 벡터 공간에서 가까이 위치할수록 의미가 비슷한 문장이 됨 (텍스트의 의미적 정보 보존)
4. **임베딩 검색 (Vector Search):** 사용자가 질문을 던지면 질문도 임베딩하여 Vector DB에서 질문 벡터와 의미적 유사성이 가장 높은 문서 Chunk들을 추출
5. **답변 생성:** 추출된 문서 조각들과 사용자 질문을 최종 프롬프트로 묶어 LLM에 전달하여 완벽한 답변 생성 (RAG의 각 단계 품질이 최종 답변의 품질을 좌우함)

* **배포 팁:** GitHub 레포지토리를 Public으로 생성한 뒤, Repository Settings -> Pages 탭에서 브랜치를 지정해주면 웹페이지로 무료 배포 가능

---

# 0604
## 머신러닝과 데이터 분석 핵심 개요
* 아무리 이론상 좋은 인공지능 모델이 나와도 컴퓨터 하드웨어가 연산 속도를 버텨주지 못하면 돌릴 수 없음
* 과거 '인공 신경망'이 가졌던 침체기와 부정적인 인식을 깨고 부활하면서 명칭을 **'Deep Learning'**으로 바꿈
* 2017년 이미지 인식 대회(ILSVRC)에서 SENet이 오답률을 4.3%까지 낮추고, 알파고가 인간을 뛰어넘으면서 대세가 됨
* 기존 전통 AI는 하나의 모델이 하나의 명확한 문제만 해결하는 구조였음

* **강화학습 (Reinforcement Learning):** 어린아이가 시행착오를 겪으며 상과 벌을 통해 성장하는 것과 유사한 방식, 에이전트가 환경과 상호작용하며 보상(Reward)을 최대화하는 방향으로 지능을 고도화함

## 데이터 분석 및 모델링 전체 프로세스
* 데이터 수집 -> EDA(탐색적 데이터 분석) -> 데이터 전처리 -> 모델 선택 -> 모델 학습 -> 예측 -> 평가 -> 배포
* 실제 프로젝트에서 모델을 골라 학습시키는 코드는 5분도 안 걸림. 가장 중요한 것은 앞단의 데이터 분석과 전처리 과정임
* **데이터 수집 방법:** 사내 데이터베이스 활용, 웹 크롤링, 공공데이터 포털 등 이용

## 학습 및 평가 데이터 분리 (과적합 방지)
* 모델의 정확한 성능 검증을 위해 전체 수집 데이터의 일부를 학습에 쓰지 않고 평가용 데이터(Test)로 분리해둠
* **과소적합 (Underfitting):** 모델이 지나치게 단순하여 학습 데이터조차 제대로 맞추지 못하고 일반화에 실패한 상태
* **과대적합 (Overfitting):** 모델이 학습 데이터에만 너무 과도하게 딱 맞춰져서(암기해 버려서) 새로운 데이터가 들어왔을 때는 엉뚱한 예측을 하는 상태, 일반화가 되지 않은 상태

## 지도학습의 2가지 핵심 분야
1. **회귀 (Regression):** 관찰된 변수들을 토대로 독립변수와 종속변수 사이의 모형을 구한 뒤 적합도를 측정하는 분석 방법. 시간에 따라 연속적으로 변화하는 데이터 예측이나 인과관계, 통계적 영향력 모델링에 사용 (예: 주가 예측, 집값 예측)
   * 독립변수(영향을 주는 변수 $X$)가 종속변수(영향을 받는 결과 변수 $Y$)에 실제로 어떤 영향을 미치는지 알아보고자 할 때 사용
2. **분류 (Classification):** 주어진 입력값이 어떤 그룹이나 카테고리에 속하는지 구별하여 나누는 문제 (예: 스팸 메일 여부 0 또는 1, 이미지 객체 분류)

## 선형 회귀 (Linear Regression)
* 가장 일반적인 회귀분석 기법으로, 독립변수 $X$와 종속변수 $Y$의 관계를 직선 형태로 표현하는 가설 함수 $H(x) = Wx + b$를 세움. 최적의 적합선(가장 데이터를 잘 대변하는 직선)을 찾는 것이 목표임
* 가설이 실제 정답과 얼마나 잘못되었는가를 계산하는 함수를 오차 함수 또는 **Cost Function(비용 함수)**이라고 함
* 비용 함수를 계산할 때 '최소제곱법'(오차를 제곱해서 평균 내기)을 주로 사용하며, 오차가 커질수록 2차 곡선 그래프 형태로 Cost 값이 급증함
* **경사하강법 (Gradient Descent):** 오차 Cost($W, b$) 값을 최소화하는 최적의 $W$(기울기)와 $b$(편향)를 찾기 위해 미분을 이용하는 알고리즘
  * Cost 함수를 미분하여 현재 위치에서의 기울기를 구한 뒤, 기울기 값만큼을 빼주면서 최저점을 향해 이동함 (기울기가 음수면 오른쪽으로 $W$ 이동, 양수면 왼쪽으로 $W$ 이동)
  * 기울기가 0이 되는 지점이 오차가 최소가 되는 지점이며 이때의 $W, b$를 찾으면 학습이 종료됨
  * 업데이트 공식: $W = W - (\text{기울기} \times \text{Learning Rate})$, $b = b - (\text{기울기} \times \text{Learning Rate})$
* **Learning Rate (학습률, lr):** 한 번에 얼마나 큰 보폭으로 기울기를 따라 내려갈지 결정하는 하이퍼파라미터. 주로 0.0001 같이 작은 값을 사용함. 너무 크면 오차 최저점을 지나쳐 발산하고, 너무 작으면 학습 속도가 너무 느려짐
* 지정한 반복 횟수인 Epoch(보통 1000회 이상) 동안 Cost가 0에 수렴할 때까지 가중치를 지속 업데이트함

* **특성 공학 (Feature Engineering):** 기존에 수집된 조건을 그냥 쓰지 않고, 도메인 지식이나 연산식을 활용하여 결합하거나 가공해 모델이 학습하기 좋은 새로운 조건(특성)을 만들어내는 방법

## 규제 (Regularization) - 과적합 방지 기법
* **릿지 (Ridge) 규제:** L2 규제 사용. 모든 독립변수들이 결과에 미치는 영향력이 전반적으로 비슷할 때 사용하며, 특정 가중치가 너무 커지지 않도록 전체적으로 가중치 크기를 작게 조절하여 편차를 줄임
* **라쏘 (Laasso) 규제:** L1 규제 사용. 독립변수들 간의 영향력 차이가 클 때 사용하며, 중요하지 않은 변수의 가중치를 완전히 0으로 만들어 모델에서 해당 특성을 제거해 버리는 효과가 있음

## 로지스틱 회귀 (Logistic Regression)
* 이름은 회귀이지만 실제로는 **'분류'** 알고리즘임
* 종속변수가 이분형(0 또는 1, True 또는 False)인 이진 분류 문제에 수행 가능
* 선형 회귀의 출력값은 음의 무한대부터 양의 무한대까지 범위를 가지기 때문에, 이를 그대로 분류 문제에 적용하면 기준선 설정이 불가능함
* 자연 현상이나 사회 현상에서 특정 이진 확률 값은 직선이 아니라 주로 S자 커브 형태를 따름. 선형 회귀의 출력 값을 S 커브 함수에 통과시켜 0과 1 사이의 확률 값으로 변환하는 방식을 취함

* **Sigmoid (시그모이드) 함수:** S자형 곡선을 가지는 대표적인 활성화 함수로, 공식은 $g(x) = \frac{1}{1 + e^{-x}}$ 임
  * 입력 $x$ 값이 아무리 커져도 출력 $g(x)$ 값은 절대 1을 넘지 못하고 0과 1 사이로 수렴함. 출력값을 확률로 해석하여 보통 0.5 이상이면 1(True), 0.5 미만이면 0(False)으로 이진 분류를 수행함

## KNN (K-Nearest Neighbors, K-최근접 이웃)
* 새로운 데이터가 입력되었을 때, 기존 데이터 공간에서 해당 데이터와 가장 가까이에 위치한 $K$개의 데이터를 확인하여 다수결 법칙으로 새로운 데이터의 특성을 파악하는 거리 기반 알고리즘
* **특징:** 직관적이고 이해하기 매우 쉬움. 사전 학습 과정이 따로 필요 없는 '게으른 학습(Lazy Learning)' 기법임. 데이터의 분포 형태에 상관없이 적용 가능하며 데이터 수가 많을수록 대조군이 늘어나 정확도가 올라갈 수 있음
* **단점:** 새로운 데이터가 올 때마다 모든 기존 데이터와의 거리를 새로 계산해야 하므로 데이터가 많을수록 연산량이 기하급수적으로 증가함. 또한 데이터의 차원이 커질수록 공간이 텅 비어버려 정확도가 급감하는 '차원의 저주' 문제가 발생함

* **거리 측정 공식 종류**
  1. **유클리드 거리 (Euclidean Distance):** 2차원 평면 위 두 점 사이의 최단 직선거리. 각 축의 차이를 빼서 제곱한 뒤 모두 더해 루트를 씌움
  2. **맨해튼 거리 (Manhattan Distance):** 격자 형태의 도로망처럼 직선이 아니라 각 축의 절대 차이값을 모두 더해 구하는 실제 이동 거리

* **최적의 K값 설정 방법**
  * 최선의 $K$를 선택하는 정답은 없으며 데이터마다 다르게 접근해야 함
  * **$K$가 너무 작다 (예: $K=1$):** 이웃 수가 너무 적어 노이즈나 이상치 하나에 예측이 요동침. 아주 불안정한 경계를 만들며 오버피팅(과적합) 발생
  * **$K$가 너무 크다:** 너무 많은 이웃을 고려하다 보니 주변의 사소한 특성을 무시하고 지나친 일반화가 일어남. 결국 모델이 둔감해지는 언더피팅(과소적합) 발생
  * **실무적 선택법:** $K$ 값을 1부터 하나씩 증가시켜 가며 전체 점들에 대해 KNN 분류를 테스트해 보고 오류율이 가장 적은 최적의 $K$ 값을 선택함 (보통 짝수가 되어 투표 동률이 나오는 걸 막기 위해 홀수로 설정함)

## 모델 평가 지표의 한계와 확장
* 단순 정확도(Accuracy)는 데이터 클래스 비율이 불균형할 때 심각한 한계가 있음 (예: 99개의 정상 데이터와 1개의 암 데이터가 있을 때 무조건 정상이라고만 예측해도 정확도는 99%가 나옴)
* **재현율 (Recall):** 실제 True인 것들 중에서 모델이 올바르게 True라고 맞춘 비율
* **정밀도 (Precision):** 모델이 True라고 예측한 것들 중에서 실제 정답이 True인 비율

* **의료 데이터 예시 상황 분석**
  * **상황 A:** 암인 줄 알았는데 암이 아닌 경우 (정밀도 저하 유발, 환자가 놀라기만 함)
  * **상황 B:** 암이 아닌 줄 알았는데 실제로는 암인 경우 (재현율 저하 유발, 암을 놓쳐 환자가 사망할 수 있어 의료계에서는 이 상황을 막기 위해 재현율을 극도로 높이는 방향을 선호함)
  * **트레이드 오프:** 재현율을 극도로 높이면(전부 다 암 환자라고 예측해 버리면) 사소한 것도 다 암이라 하므로 정밀도가 매우 낮아짐
* **F1-Score:** 정밀도와 재현율 중 어느 한쪽으로 치우치지 않도록 두 지표의 조화평균을 구한 값 (성능을 객관적으로 비교할 수 있는 통합 지표)
* **Precision-Recall Curve:** 임계값 변화에 따른 정밀도와 재현율의 곡선 변화를 나타낸 그래프

* **기타 주요 성능 지표**
  * **mAP (Mean Average Precision):** 컴퓨터 비전 객체 탐지 분야에서 각 클래스별 AP(정밀도 곡선 아래 면적)를 구한 뒤 이들의 전체 평균을 낸 핵심 지표
  * **Top-5 Accuracy:** 모델이 예측한 확률 상위 5개 후보 안에 실제 정답이 들어있으면 정답으로 인정해 주는 지표 (클래스가 엄청 많은 ImageNet 등에서 사용하며, 다소 널널한 지표임)
  * **Top-1 Accuracy:** 오직 확률이 가장 높은 1등 예측값과 실제 정답이 일치해야만 인정하는 엄격한 지표
  * **Confusion Matrix (혼동 행렬):** 실제 정답과 모델의 예측 결과를 True Positive, False Positive, False Negative, True Negative 4가지 격자로 나누어 한눈에 매트릭스로 파악할 수 있게 만든 평가 표

---

# 0611
## SVM (서포트 벡터 머신)
* 데이터들을 서로 다른 카테고리로 분리할 때, 오차를 가지지 않으면서 분리 경계선과 데이터 간의 거리인 **Margin(여백)**을 최대화하는 방향으로 결정 경계(초평면)를 찾는 알고리즘
* **분류에서의 여백:** 분리하는 직선과 가장 가까운 데이터 사이의 최적 거리 여백을 최대화함
* **회귀에서의 여백:** 결정 경계 직선을 중심으로 양옆에 점선으로 파이프 모양의 여백 공간을 만들고, 가급적 많은 데이터가 이 여백 안(양 점선 사이의 수평거리 공간)에 들어오도록 유도함

## 마진의 종류와 커널 트릭
* **소프트 마진 (Soft Margin):** 이상치가 결정 경계를 조금씩 넘어가는 것을 어느 정도 유연하게 허용하는 경계선 (노이즈가 많은 실제 데이터에 적합)
* **하드 마진 (Hard Margin):** 이상치를 절대 허용하지 않고 완벽하게 두 그룹을 가르는 방식 (오버피팅 위험이 큼)
* **Kernel Trick (커널 트릭):** 원래 2차원 저차원 공간에서는 직선 하나로 도저히 선형 분리가 불가능한 데이터 분포일 때, 수학적 함수를 통해 데이터를 강제로 고차원 공간으로 보냄. 고차원에서는 선형 분리가 가능해지므로 이를 이용해 비선형 분류 경계를 만들어내는 강력한 기술

## Tree 기반 모델과 앙상블 기법
* **앙상블 학습:** 하나의 강력한 모델을 쓰는 대신, 여러 개의 분류기(약한 모델)를 생성한 뒤 그들의 예측 결과를 결합하여 보다 정확한 최종 예측을 도출하는 기법 (종류: 보팅, 배깅, 부스팅)

* **Voting (보팅):** 서로 다른 알고리즘을 가진 여러 개의 모델(예: KNN, 로지스틱 회귀, SVM 등)을 조합하여 같은 데이터를 주고 투표를 통해 최종 결과를 결정함
  * **Hard Voting:** 다수의 분류기가 예측한 결과 노출 값 중 가장 많이 나온 레이블을 최종 다수결로 선정 (일반적)
  * **Soft Voting:** 모든 분류기가 각 레이블별로 예측한 결정 확률들의 평균을 구한 뒤, 평균 확률이 가장 높은 레이블을 최종 결과로 선정

* **Bagging (배깅):** 모두 같은 유형의 알고리즘(주로 의사결정나무)을 사용함. 원본 데이터에서 중복을 허용하여 무작위로 데이터를 쪼개어 샘플링(부트스트랩)한 뒤 각각 모델을 학습시키고 결과를 하나로 집계(Aggregating)함
  * 최종 결정 방식: 범주형(분류) 문제는 다수결로 결정하고, 연속형(회귀) 문제는 각 나무들의 예측 평균값으로 결정함. 데이터를 분할 학습하므로 모델의 과적합(오버피팅)을 효과적으로 방지함

* **Boosting (부스팅):** 여러 개의 분류기를 사용하되 동시 학습이 아니라 순차적(Sequential)으로 학습을 수행함. 이전 분류기가 예측에 틀린 오답 데이터에 대해 올바르게 예측할 수 있도록 다음 분류기에 가중치(Weight)를 부여하면서 학습과 예측을 계속 진행함
  * **특징:** 단계를 통과할수록 모델이 맞추기 어려운 까다로운 데이터에 집중함. 배깅에 비해 예측 성능과 정확도가 압도적으로 뛰어나 앙상블 학습을 주도하고 있으나, 순차 연산이라 속도가 느리고 노이즈 데이터에 과적합될 가능성도 존재함. 상황에 맞게 적절히 사용해야 함
  * **대표 키워드 알고리즘:** XGBoost, AdaBoost, Gradient Boosting

## 의사결정나무 (Decision Tree)
* 분류와 회귀 문제에 모두 사용 가능하며, 입력 변수들을 특정 기준에 따라 스무고개 하듯이 반씩 분기해 나가는 트리 형태의 알고리즘
* 나무 구조와 유사하게 생겼으며 최대한 집단의 순도가 높아지도록(비슷한 성격끼리 뭉치도록) 반으로 가르는 조건을 설정함
* **구조:** Root Node (뿌리 노드) -> 상위 부모 노드 -> 자식 노드 -> 최종 분기가 끝나는 Leaf Node (끝 노드)
* **장점:** 모델의 결과가 매우 직관적이고 해석하기 쉬움. 변수의 영역 흐름을 따라 조건문을 타기 때문에 인과관계 파악 용이
* **단점:** 예측력이 다소 떨어짐. 하단 노드에서 평균이나 다수결로 최종 출력을 내기 때문에 회귀 예측 시 오류가 누적되어 한 군데 에러가 나면 밑의 모든 노드에 에러가 전파 및 누적되는 취약점이 있음

## Random Forest (랜덤 포레스트)
* 의사결정나무의 단점을 보완하기 위해 나무를 임의적으로 수십~수백 개 생성하여 학습시키는 대표적인 배깅 앙상블 모델. 여러 나무의 선형 결합으로 최종 예측을 수행함
* **핵심 특징 4가지**
  1. **임의성:** 서로 조금씩 다른 특성을 가진 트리들을 무작위로 구성함
  2. **비상관화:** 각 트리들의 예측이 서로 연관되지 않고 독립적이라 오류가 한 번에 전파되지 않고 견고함
  3. **노이즈에 강함:** 일부 나무가 엉뚱한 예측을 해도 수많은 다른 나무들의 다수결(평균)에 의해 묻히므로 에러 누적 차단
  4. **일반화 성능 우수:** 임의화 과정을 통해 의사결정나무의 치명적 약점인 과적합을 완벽히 극복함
* **장점:** 과적합이 거의 일어나지 않고 결측치와 이상치에 매우 강함. 데이터 정규화나 스케일링 같은 고정 전처리가 필수로 요구되지 않으며, 새로운 데이터가 유입되어도 전체 판도에 크게 영향을 받지 않음
* **단점:** 수많은 트리를 다 연산해야 하므로 트리 개수가 많아질수록 컴퓨터의 계산 시간과 연산량 부담이 커짐

## 비지도 학습 (Unsupervised Learning) - 군집화
* 지도학습은 정답 라벨이 지정된 대량의 데이터가 필요하고 객체를 분류하는 것이 타당하지만, 비지도학습은 정답(Label)이 지정되어 있지 않은 데이터 세트를 학습 데이터 없이 자체 분석하는 기법임
* 마치 아무 정보 없이 카드를 비슷한 것끼리 나누는 것처럼, 정답은 없지만 데이터 자체의 특징과 거리를 분석해 여러 그룹으로 묶어냄 (가장 흔한 예시: MBTI 분류 데이터 구조 분석)

## K-Means (K-평균 군집화)
* 평균을 활용하여 사전에 사용자가 지정한 $K$개의 군집(Cluster)을 만드는 대표적인 알고리즘. 군집의 중심 좌표와 군집 안 데이터 사이의 평균 거리를 계산하며 작동함
* **작동 프로세스:**
  1. 사용자가 설정한 $K$개의 군집 개수만큼 공간에 초기 중심점(Centroid)을 랜덤하게 선택함
  2. 각 데이터들은 거리상 가장 가까운 중심점이 있는 군집으로 일차 소속을 재지정함
  3. 소속이 정해지면 해당 군집에 속한 데이터들의 실제 평균 위치를 계산해 중심점을 그 평균 위치로 재설정함
  4. 중심점의 위치가 더 이상 변하지 않고 고정될 때까지 2번과 3번 과정을 무한 반복 후 종료함
* **특징:** 알고리즘이 매우 쉽고 간결하여 널리 쓰임. 그러나 군집의 개수 $K$값을 사용자가 초기에 직접 지정해야 하는 부담이 있으며, 속성(차원)의 개수가 너무 많아지면 거리 기반이라 정확도가 급격히 감소함

## DBSCAN (Density-Based Spatial Clustering)
* K-Means와 달리 숫자를 지정하지 않고, 단위 공간 안에 촘촘하게 모여있는 데이터들의 '밀도(Density)'를 기반으로 스스로 군집화를 수행하는 알고리즘
* 일정 거리(Epsilon) 안에 샘플 개수가 일정 수(MinPoints) 이상 모여 있으면 하나의 군집으로 묶어나가는 방식
* **작동 프로세스:**
  1. 데이터 중 임의의 하나의 포인트를 선택함
  2. 선택한 데이터 포인트로부터 반지름 Epsilon(에프실론) 거리 내에 존재하는 모든 데이터를 탐색함
  3. 해당 거리 내의 데이터 수가 사용자가 정한 최소 개수인 MinPoints 이상이면 그 포인트를 중심으로 군집을 형성함
  4. 이웃 포인트들도 밀도 조건을 만족하면 이웃의 이웃까지 연결하여 두 군데 영역을 하나의 거대한 군집으로 합치며 확장해 나감
* **특징:** K-Means와 달리 기하학적이고 복잡한 분포(초승달 모양 등)를 가진 데이터 세트에 대해 성능이 매우 우수함. 또한 군집의 개수를 미리 지정할 필요가 없으며, 밀도 조건에 아예 속하지 못하고 외딴곳에 떨어져 있는 찌꺼기 요소를 군집에서 과감히 제외하는 '이상치(Noise) 탐지 및 정리' 기능이 자체적으로 포함되어 있음