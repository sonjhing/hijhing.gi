# 3. 다중회귀분석 (Multiple Linear Regression)

## 1) 개념

다중회귀분석(Multiple Linear Regression)은
두 개 이상의 독립변수(X)가 하나의 종속변수(Y)에 미치는 영향을 분석하는 회귀 기법이다.

즉,

> "여러 원인이 결과에 동시에 어떤 영향을 미치는가?"
> "여러 정보를 활용하여 결과를 더 정확하게 예측할 수 있는가?"

를 분석하는 방법이다.

단순 선형회귀분석은 독립변수가 1개였지만,
다중회귀분석은 독립변수가 여러 개라는 차이가 있다.

---

# 2) 기본 수식

다중회귀분석의 기본식은 다음과 같다.

[
Y = a + b_1X_1 + b_2X_2 + b_3X_3 + \cdots + e
]

| 기호         | 의미            |
| ---------- | ------------- |
| Y          | 종속변수(결과값)     |
| X₁, X₂, X₃ | 독립변수(원인)      |
| a          | 절편(Intercept) |
| b₁, b₂, b₃ | 각 변수의 회귀계수    |
| e          | 오차(Error)     |

---

## 수식의 의미

* 각 독립변수는 결과값(Y)에 서로 다른 영향을 준다.
* 회귀계수(b)는 해당 변수가 Y에 미치는 영향력을 의미한다.
* 다른 변수는 고정된 상태에서 특정 변수의 영향만 해석할 수 있다.

---

# 3) 직관적 예시

## 예시 1 : 시험 점수 예측

시험 점수(Y)가 아래 요소들의 영향을 받는다고 가정하자.

* X₁ = 공부시간
* X₂ = 수면시간
* X₃ = 출석률

회귀식이 다음과 같이 나왔다고 가정한다.

[
Y = 30 + 4X_1 + 2X_2 + 0.5X_3
]

### 해석

* 기본 점수는 30점
* 공부시간 1시간 증가 → 점수 4점 증가
* 수면시간 1시간 증가 → 점수 2점 증가
* 출석률 1% 증가 → 점수 0.5점 증가

즉, 시험 점수는 여러 요인의 영향을 동시에 받는다고 해석할 수 있다.

---

## 예시 2 : 아파트 가격 예측

아파트 가격(Y)에 영향을 주는 변수들:

* X₁ = 평수
* X₂ = 역과의 거리
* X₃ = 건축 연수

회귀식:

[
가격 = 2억 + 800만X_1 - 300만X_2 - 500만X_3
]

### 해석

* 평수가 클수록 가격 증가
* 지하철역과 멀수록 가격 감소
* 건물이 오래될수록 가격 감소

처럼 여러 요소를 동시에 고려하여 가격을 예측한다.

---

# 4) 다중회귀분석의 특징

## 장점

### ① 현실 데이터를 더 잘 설명

현실의 문제는 하나의 원인만으로 설명되지 않는 경우가 많다.

예:

* 매출 = 광고비 + 가격 + 계절 + 경쟁사 영향
* 성적 = 공부시간 + 수면 + 집중력 + 출석률

따라서 다중회귀는 현실 문제를 더 잘 표현할 수 있다.

---

### ② 예측 성능 향상

독립변수를 여러 개 사용하므로 단순회귀보다 더 정확한 예측이 가능하다.

---

### ③ 변수별 영향력 분석 가능

어떤 변수가 결과에 더 큰 영향을 미치는지 확인할 수 있다.

예:

* 공부시간 영향 > 수면시간 영향
* 광고비 영향 > 할인율 영향

---

# 5) 다중회귀분석의 한계

## ① 과대적합 가능성

변수가 너무 많아지면 훈련 데이터에만 지나치게 맞춰질 수 있다.

---

## ② 다중공선성 문제

독립변수끼리 서로 강한 상관관계를 가지면 문제가 발생할 수 있다.

예:

* 키와 몸무게
* 나이와 경력

이 경우 변수의 영향력을 정확하게 해석하기 어려워진다.

이를 다중공선성(Multicollinearity)이라고 한다.

---

## ③ 해석이 복잡해짐

변수가 많아질수록 모델 구조와 결과 해석이 어려워질 수 있다.

---

# 6) 단순회귀와 다중회귀 비교

| 구분      | 단순회귀    | 다중회귀      |
| ------- | ------- | --------- |
| 독립변수 개수 | 1개      | 2개 이상     |
| 관계 표현   | 단순      | 복합        |
| 해석 난이도  | 쉬움      | 상대적으로 어려움 |
| 예측 성능   | 낮을 수 있음 | 더 높아질 가능성 |

---

# 7) 머신러닝에서의 의미

다중회귀분석은 머신러닝의 가장 기본적인 예측 모델 중 하나이다.

특히 아래와 같은 분야에서 많이 사용된다.

| 분야  | 활용 예시    |
| --- | -------- |
| 부동산 | 집값 예측    |
| 금융  | 주가 예측    |
| 의료  | 질병 위험 예측 |
| 마케팅 | 매출 예측    |
| 교육  | 성적 예측    |

---

# 8) 핵심 정리

다중회귀분석은

> 여러 개의 원인(X)이 결과(Y)에 어떤 영향을 미치는지 분석하는 기법이다.

핵심 수식:

[
Y = a + b_1X_1 + b_2X_2 + \cdots
]

즉,

* 여러 변수의 영향을 동시에 분석하고
* 결과를 예측하며
* 변수별 영향력을 해석

할 수 있는 대표적인 통계 및 머신러닝 기법이다.
