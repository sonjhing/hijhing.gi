# 회귀분석 (Regression Analysis)

회귀분석(Regression Analysis)은 하나 이상의 독립변수(원인)가 종속변수(결과)에 어떤 영향을 미치는지 분석하고, 이를 바탕으로 미래의 값을 예측하는 대표적인 통계 분석 기법이다.

즉,

* 어떤 요인이 결과에 영향을 주는지 파악하고
* 그 관계를 수식으로 표현하며
* 새로운 데이터에 대한 결과를 예측

하는 것이 회귀분석의 핵심 목적이다.

---

# 1. 다중회귀분석 (Multiple Linear Regression)

## 1) 개념

다중회귀분석(Multiple Linear Regression)은 두 개 이상의 독립변수(X)가 하나의 종속변수(Y)에 미치는 영향을 분석하는 회귀 기법이다.

쉽게 말하면,

> 여러 원인이 결과에 동시에 어떤 영향을 미치는가?
>
> 여러 정보를 활용하여 결과를 더 정확하게 예측할 수 있는가?

를 분석하는 방법이다.

단순회귀분석은 독립변수가 1개였지만, 다중회귀분석은 독립변수가 여러 개라는 차이가 있다.

---

## 2) 기본 수식

```text
Y = a + b₁X₁ + b₂X₂ + b₃X₃ + e
```

| 기호         | 의미            |
| ---------- | ------------- |
| Y          | 종속변수(결과값)     |
| X₁         | 독립변수 1        |
| X₂         | 독립변수 2        |
| X₃         | 독립변수 3        |
| a          | 절편(Intercept) |
| b₁, b₂, b₃ | 회귀계수          |
| e          | 오차(Error)     |

### 수식의 의미

* 각 독립변수는 결과값(Y)에 서로 다른 영향을 준다.
* 회귀계수는 해당 변수가 결과에 미치는 영향력을 의미한다.
* 다른 변수들이 고정된 상태에서 특정 변수의 영향력을 해석할 수 있다.

---

## 3) 실습 예제

이번 실습에서는 시험점수에 영향을 주는 세 가지 요인을 사용한다.

| 변수   | 역할       |
| ---- | -------- |
| 공부시간 | 독립변수(X₁) |
| 학원수  | 독립변수(X₂) |
| 과외여부 | 독립변수(X₃) |
| 시험점수 | 종속변수(Y)  |

즉,

```text
시험점수 =
a +
b₁ × 공부시간 +
b₂ × 학원수 +
b₃ × 과외여부
```

형태의 회귀모델을 학습하게 된다.

---

# 실습 : 공부시간, 학원수, 과외여부를 이용한 시험점수 예측

## 1) 데이터 로드

```python
import pandas as pd

study = pd.read_csv(
    './머신러닝실습용자료/공부시간과시험점수2.csv',
    encoding='cp949'
)

study
```

### 변수 설명

| 변수   | 설명      |
| ---- | ------- |
| 이름   | 식별용 데이터 |
| 공부시간 | Feature |
| 학원수  | Feature |
| 과외여부 | Feature |
| 시험점수 | Target  |

---

## 2) 훈련용 데이터셋과 테스트용 데이터셋 분리

```python
from sklearn.model_selection import train_test_split

# data, target 지정
# 다중회귀분석의 경우 data를 여러개 설정.
data = study[['공부시간', '학원수','과외여부']]
target = study['시험점수']

# train, test 데이터 분리
훈련용_data, 테스트용_data, 훈련용_target, 테스트용_target = train_test_split(
    data,
    target,
    test_size=0.2,
    random_state=40
)
```

### 설명

* 독립변수(X)는 3개이다.
* 종속변수(Y)는 시험점수이다.
* 전체 데이터 중 80%는 학습용, 20%는 평가용으로 사용한다.

---

## 3) 선형회귀 모델 학습

```python
from sklearn.linear_model import LinearRegression

lr = LinearRegression()

lr.fit(
    훈련용_data,
    훈련용_target
)
```

### 설명

LinearRegression 모델을 생성하고 학습 데이터를 이용하여 회귀모델을 학습한다.

---

## 4) 시험점수 예측

```python
lr.predict([[13,5,0]])
```

### 입력값 의미

| 값  | 의미        |
| -- | --------- |
| 13 | 공부시간 13시간 |
| 5  | 학원수 5개    |
| 0  | 과외 안 함    |

### 결과

```python
array([84.86657458])
```

### 해석

공부시간이 13시간이고, 학원을 5개 수강하며, 과외를 받지 않는 학생의 예상 시험점수는 약 84.87점으로 예측된다.

---

## 5) 모델 성능 평가

```python
lr.score(
    테스트용_data,
    테스트용_target
)
```

### 결과

```python
0.8967399286768529
```

### 해석

R²(결정계수)가 약 0.8967이다.

즉,

* 시험점수 변동의 약 89.67%를
* 공부시간
* 학원수
* 과외여부

세 가지 변수로 설명할 수 있다는 의미이다.

일반적으로 0.8 이상의 R² 값은 설명력이 높은 모델로 평가된다.

---

# 다중회귀분석의 장점

### ① 현실 데이터를 더 잘 설명

현실의 문제는 하나의 원인만으로 설명되지 않는다.

예)

* 성적 = 공부시간 + 학원수 + 과외여부
* 매출 = 광고비 + 가격 + 계절
* 집값 = 평수 + 위치 + 건축연수

---

### ② 예측 성능 향상

여러 정보를 동시에 활용하므로 단순회귀보다 더 정확한 예측이 가능하다.

---

### ③ 변수별 영향력 분석 가능

어떤 변수가 결과에 더 큰 영향을 미치는지 확인할 수 있다.

---

# 다중회귀분석의 한계

### ① 과대적합 가능성

변수가 지나치게 많아지면 훈련 데이터에만 맞춰질 수 있다.

---

### ② 다중공선성 문제

독립변수들끼리 강한 상관관계를 가지면 회귀계수 해석이 어려워질 수 있다.

예)

* 키와 몸무게
* 나이와 경력

---

### ③ 해석이 복잡해짐

변수가 많아질수록 모델 구조와 결과 해석이 어려워진다.

---

# 핵심 정리

* 다중회귀분석은 여러 개의 독립변수를 이용하여 하나의 결과를 예측하는 기법이다.
* 이번 실습에서는 공부시간, 학원수, 과외여부를 사용하여 시험점수를 예측하였다.
* 예측 결과는 약 84.87점으로 나타났다.
* 테스트 세트의 R² 점수는 약 0.8967로 높은 설명력을 보였다.
* 다중회귀분석은 현실 문제를 더 잘 설명할 수 있으며 머신러닝의 대표적인 예측 모델 중 하나이다.
