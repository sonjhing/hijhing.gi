# K-Means와 DBSCAN 군집분석 비교 실습

## 📌 K-Means와 DBSCAN이란 무엇인가요?

머신러닝의 비지도 학습(정답이 없는 데이터를 묶어주는 학습) 중 가장 대표적인 두 가지 알고리즘입니다.

1. **K-Means (K-평균):** 중심점을 찍고, 가장 가까운 데이터들을 원 모양으로 묶어가는 방식입니다. (거리 기반)
2. **DBSCAN (밀도 기반 군집화):** 데이터가 촘촘하게(밀도 높게) 몰려있는 부분을 하나로 묶고, 듬성듬성한 곳은 경계로 삼는 방식입니다. (밀도 기반)

## 🎯 언제 사용할까요?

* **K-Means 사용 시기:** 데이터가 비교적 동그란(원형) 형태로 뭉쳐있을 때, 그리고 내가 몇 개의 그룹(K)으로 나눌지 명확히 알고 있을 때 사용합니다.
* **DBSCAN 사용 시기:** 초승달 모양이나 불규칙하고 복잡한 형태로 데이터가 퍼져 있을 때, 그리고 동떨어진 데이터(이상치, 노이즈)를 찾아내어 걸러내고 싶을 때 매우 유용합니다. 몇 개의 그룹이 나올지 미리 정할 필요가 없습니다.

---

## 1. 테스트용 데이터 생성

비선형(동그랗지 않은) 데이터에 대한 두 알고리즘의 차이를 극명하게 보기 위해, `make_moons()` 함수를 사용하여 두 개의 반달(Moon)이 겹쳐진 형태의 가짜 데이터를 생성합니다.

```python
import matplotlib.pyplot as plt
from matplotlib import style
from sklearn.datasets import make_moons

# 반달 모양의 데이터(X)와 각 데이터가 속한 정답 라벨(y)을 생성합니다.
# n_samples=400 : 총 400개의 점(데이터)을 만듭니다.
# noise=0.1 : 반달 모양이 너무 매끄럽지 않도록 약간의 무작위 노이즈(오차)를 10% 섞어줍니다.
# random_state=10 : 코드를 다시 실행해도 똑같은 반달 모양이 나오도록 난수를 고정합니다.
X, y = make_moons(
    n_samples=400,
    noise=0.1,
    random_state=10
)

# 생성된 데이터(X)를 산점도 그래프로 그립니다.
# X[:, 0]은 가로축(첫 번째 특징), X[:, 1]은 세로축(두 번째 특징)을 의미합니다.
plt.scatter(X[:, 0], X[:, 1])

# 그래프를 화면에 보여줍니다.
plt.show()
```

---

## 2. 군집 결과 시각화 함수 생성

모델이 데이터를 두 그룹으로 나누었을 때, 이를 색상과 모양으로 구분하여 시각적으로 쉽게 확인하기 위한 전용 함수를 미리 만들어 둡니다.

```python
import matplotlib.pyplot as plt

# X: 데이터, y: 군집 예측 결과(라벨), title: 그래프 상단 제목
def cluster_result(X, y, title):
    # 첫 번째 군집(y == 0)에 해당하는 데이터만 골라서 그립니다.
    plt.scatter(
        X[y == 0, 0], # y가 0인 데이터의 X좌표
        X[y == 0, 1], # y가 0인 데이터의 Y좌표
        c='green',    # 색상은 초록색
        marker='o',   # 마커 모양은 동그라미
        s=40,         # 점의 크기
        label='Cluster_1' # 범례에 표시할 이름
    )

    # 두 번째 군집(y == 1)에 해당하는 데이터만 골라서 그립니다.
    plt.scatter(
        X[y == 1, 0], # y가 1인 데이터의 X좌표
        X[y == 1, 1], # y가 1인 데이터의 Y좌표
        c='red',      # 색상은 빨간색
        marker='s',   # 마커 모양은 네모(square)
        s=40,
        label='Cluster_2'
    )

    # 파라미터로 받은 문자열을 그래프 제목으로 설정합니다.
    plt.title(title)
    
    # 우측 상단 등에 범례(label)를 표시합니다.
    plt.legend()
    
    # 완성된 그래프를 보여줍니다.
    plt.show()
```

---

## 3. K-Means 클러스터링 수행

거리(중심점) 기반의 K-Means 알고리즘이 반달 모양 데이터를 어떻게 나누는지 확인해 봅니다.

```python
from sklearn.cluster import KMeans

# K-Means 모델 객체를 생성합니다.
# n_clusters=2 : 2개의 그룹으로 나누겠다고 미리 설정합니다.
km = KMeans(
    n_clusters=2,
    random_state=10
)

# fit_predict 함수를 써서, 학습과 동시에 각 데이터가 0번 또는 1번 그룹 중 어디 속하는지 예측합니다.
y_km = km.fit_predict(X)

# 우리가 미리 만들어둔 함수를 호출하여 K-Means의 분류 결과를 눈으로 확인합니다.
cluster_result(
    X,
    y_km,
    title='K-Means'
)
```

### K-Means의 결과 분석 및 특징

* **결과 분석:** 반달 모양을 전혀 인식하지 못하고, 마치 칼로 무 자르듯 화면을 직선으로 위아래/좌우로 반으로 갈라버립니다.
* 중심점(Centroid)을 기준으로 둥글게 원형 영역으로 묶어버리려고 하기 때문입니다.
* 복잡하고 굽어진 형태(비선형)의 데이터에서는 K-Means의 성능이 처참하게 무너짐을 보여줍니다.

---

## 4. DBSCAN 클러스터링 수행

밀도(촘촘함) 기반의 DBSCAN 알고리즘을 사용하여 같은 데이터를 군집화합니다.

```python
from sklearn.cluster import DBSCAN

# DBSCAN 모델 객체를 생성합니다.
db = DBSCAN(
    eps=0.2,            # 이웃으로 판단할 최대 거리 반경 (반경 0.2 내에 있는 데이터만 탐색)
    min_samples=15,     # 하나의 군집(그룹)으로 인정받기 위해 모여있어야 할 최소 데이터 개수(15개)
    metric='euclidean'  # 거리를 재는 방식 (일반적인 직선거리인 유클리드 거리 사용)
)

# 학습과 동시에 각 데이터가 어느 군집에 속하는지 예측합니다.
y_db = db.fit_predict(X)

# DBSCAN의 결과를 시각화합니다.
cluster_result(
    X,
    y_db,
    title='DBSCAN'
)
```

### DBSCAN의 주요 파라미터 팁

* `eps` (Epsilon): 기준점으로부터 반경을 얼마까지로 할 것인지 정합니다. 너무 작으면 모든 데이터가 노이즈(이상치) 처리되고, 너무 크면 전체가 하나의 그룹이 되어버립니다.
* `min_samples`: 반경 내에 몇 개의 점이 모여야 어엿한 군집으로 인정할지 정합니다. 이 조건에 못 미치는 듬성듬성한 데이터는 모두 불량/노이즈(Outlier, 보통 -1 라벨)로 처리됩니다.

### DBSCAN의 결과 분석 및 특징

* **결과 분석:** 데이터가 촘촘하게 이어져 있는 길을 따라가며 그룹을 형성하므로, 두 개의 반달 모양을 완벽하게 분리해 냅니다.
* K-Means처럼 군집 개수를 2개라고 미리 알려주지 않아도 스스로 밀도를 파악해 두 그룹을 찾아냅니다.
* 뚝 떨어져 있는 이상치(Noise)를 찾아내 무시하는 기능이 있습니다.
* 복잡하고 불규칙한 비선형 구조의 데이터에 매우 강력합니다.

---

## 5. 최종 결과 요약 비교

| 비교 항목 | K-Means (K-평균) | DBSCAN (밀도 기반) |
| :--- | :--- | :--- |
| **군집 기준** | 거리 (중심점 기준) | 밀도 (데이터가 밀집된 정도) |
| **군집 개수(K) 지정** | **반드시 필요함 (단점)** | **불필요 (스스로 찾음)** |
| **이상치(Noise) 탐지** | 모든 데이터를 강제로 어딘가에 소속시킴 | 동떨어진 데이터를 찾아내 배제 가능 (-1 반환) |
| **계산 속도** | 빠름 | 상대적으로 느림 |
| **데이터 모양 특성** | 동그란 구형 데이터에 적합 | 불규칙적이고 구불구불한 형태에 강함 |
| **반달 데이터 결과** | **매우 낮음 (형태 무시)** | **매우 높음 (형태 파악 완벽)** |

### 💡 실습 결론

K-Means가 항상 정답은 아닙니다. 데이터가 원형으로 예쁘게 뭉쳐있지 않고 **반달(Moon) 형태처럼 복잡하게 꼬여있는 경우, K-Means보다 DBSCAN이 데이터의 실제 밀도 구조를 반영하여 훨씬 더 우수한 군집화 성능을 보여줍니다.**
